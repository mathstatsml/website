import { Layout } from "~/components/layouts/layout";
import { Graph } from "~/components/sections/graph";

const Home = () => {
  return (
    <Layout>
      <Graph />
    </Layout>
  );
};

export default Home;
